{
  "name": "demo1",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "serve:signal": "set SCRIPT_PATH=signaling-server.js && node ./run/serve.js",
    "serve:peer": "set SCRIPT_PATH=peer.js && node ./run/serve.js",
    "serve:signal:unix": "set -a && SCRIPT_PATH=signaling-server.js && ./run/_serve.sh",
    "serve:signal:win": "set SCRIPT_PATH=signaling-server.js && .\\run\\_serve.bat",
    "serve:peer:unix": "set -a && SCRIPT_PATH=peer.js && ./run/_serve.sh",
    "serve:peer:win": "set SCRIPT_PATH=peer.js && .\\run\\_serve.bat",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "nanoid": "^5.1.5",
    "werift": "^0.22.2",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2"
  }
}
