{
  "name": "demo1",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "serve:unix": "set -a && . .env && node --enable-source-maps dist/${SCRIPT_PATH}",
    "serve:win": "call .env.bat && node --enable-source-maps dist/%SCRIPT_PATH%",
    "serve:signal:unix": "./run/serve-signal.sh",
    "serve:signal:win": ".\\run\\serve-signal.bat",
    "serve:peer:unix": "./run/serve-peer.sh",
    "serve:peer:win": ".\\run\\serve-peer.bat",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "nanoid": "^5.1.5",
    "node-datachannel": "^0.29.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
